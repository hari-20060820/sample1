"use client";import {createContext,useContext,useEffect,useState} from "react";import type {Wishlist,WishlistItem} from "@ecommerce/shared";import {getGuestWishlist,saveGuestWishlist} from "@/lib/guestStorage";
type Ctx={wishlist:Wishlist|null;add:(id:string)=>void;remove:(id:string)=>void};const C=createContext<Ctx>({wishlist:null,add:()=>{},remove:()=>{}});export function WishlistProvider({children}:{children:React.ReactNode}){const[wishlist,setWishlist]=useState<Wishlist|null>(null);useEffect(()=>{setWishlist(getGuestWishlist()??{id:"guest",ownerId:null,name:"My finds",items:[],createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),version:0})},[]);const update=(fn:(w:Wishlist)=>Wishlist)=>setWishlist(w=>{if(!w)return w;const n=fn(w);saveGuestWishlist(n);return n});return <C.Provider value={{wishlist,add:id=>update(w=>w.items.some(i=>i.productId===id)?w:{...w,version:w.version+1,items:[...w.items,{productId:id,quantity:1,addedAt:new Date().toISOString(),source:["guest"]}]}),remove:id=>update(w=>({...w,version:w.version+1,items:w.items.filter(i=>i.productId!==id)}))}}>{children}</C.Provider>}export const useWishlist=()=>useContext(C);
