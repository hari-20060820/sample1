"use client";import {useRouter,useSearchParams} from "next/navigation";export function FilterSidebar(){const r=useRouter(),p=useSearchParams();const set=(key:string,value:string)=>{const s=new URLSearchParams(p.toString());value?s.set(key,value):s.delete(key);r.replace(`/?${s}`)};return <aside className="space-y-5 text-sm"><div><label className="block font-bold">Category</label><select defaultValue={p.get("category")??""} onChange={e=>set("category",e.target.value)} className="mt-2 w-full border bg-paper p-2"><option value="">All goods</option><option>Home</option><option>Kitchen</option><option>Wear</option></select></div><div><label className="block font-bold">Minimum rating</label><select defaultValue={p.get("minRating")??""} onChange={e=>set("minRating",e.target.value)} className="mt-2 w-full border bg-paper p-2"><option value="">Any rating</option><option value="4">4 stars & up</option><option value="3">3 stars & up</option></select></div><label className="flex gap-2"><input type="checkbox" defaultChecked={p.get("inStock")==="true"} onChange={e=>set("inStock",String(e.target.checked))}/> Ready to ship</label></aside>}
